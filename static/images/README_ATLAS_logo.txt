Place the provided ATLAS logo image file here as `atlas_logo.png` (recommended dimensions: 320x90 or similar high-res PNG). The templates will use the uploaded school logo (`SchoolProfile.logo`) if available; otherwise they fallback to this static image path:

static/images/atlas_logo.png

If you already uploaded the logo via the admin to `SchoolProfile.logo`, no further action is needed.
